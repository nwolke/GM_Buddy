# Local environment overrides (copy this to .env.local)
# This file contains secrets and should NOT be committed to git
# .env.local is gitignored and will override values from .env

# ===========================================
# AWS Cognito Configuration (SECRETS)
# ===========================================
COGNITO_REGION=us-west-2
COGNITO_USER_POOL_ID=us-west-2_XXXXXXXXX
COGNITO_CLIENT_ID=your_client_id_here
COGNITO_CLIENT_SECRET=your_client_secret_here
COGNITO_DOMAIN=your-cognito-domain-prefix

# ===========================================
# Enable Cognito for React app
# ===========================================
USE_COGNITO=true

# ===========================================
# Database passwords (if different from .env)
# ===========================================
# POSTGRES_PASSWORD=your_secure_password
