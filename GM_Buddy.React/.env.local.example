# Local development secrets (copy this to .env.local)
# This file is gitignored and overrides .env.development values
# 
# Get these values from AWS Cognito Console or your team's secrets manager

# Cognito Hosted UI domain (full URL format)
VITE_COGNITO_DOMAIN=your-domain.auth.us-west-2.amazoncognito.com

# Cognito App Client ID
VITE_COGNITO_CLIENT_ID=your_client_id_here

# Enable Cognito authentication (set to 'true' to use real login)
VITE_USE_COGNITO=true
