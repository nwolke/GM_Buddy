# ===========================================
# Local Development Environment Variables
# ===========================================
# This file is tracked in git and contains safe default values for local development

# Backend API URL
VITE_API_URL=http://localhost:8080

# Cognito Configuration (disabled by default for local dev)
VITE_USE_COGNITO=false
VITE_COGNITO_DOMAIN=
VITE_COGNITO_CLIENT_ID=
VITE_COGNITO_REDIRECT_URI=http://localhost:3000/callback
VITE_COGNITO_LOGOUT_URI=http://localhost:3000
