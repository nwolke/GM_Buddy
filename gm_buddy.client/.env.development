# Local development environment variables for Vite
# Prefix with VITE_ to expose them to the client bundle

# Set to 'false' to bypass proxy and use direct URLs (not recommended for Docker)
# When true (default), API calls use relative URLs and the Vite proxy handles routing
VITE_USE_PROXY=true

# Authorization server (JWT issuer, login, etc.)
# Only used when VITE_USE_PROXY=false
# Use port 7279 when running Authorization server in HTTPS mode (see launchSettings.json)
# VITE_AUTH_API_BASE_URL=https://localhost:7279

# Main API server (NPC data, game logic, etc.)
# Only used when VITE_USE_PROXY=false
# Use port 5001 when running Server in HTTPS mode (see launchSettings.json)
# VITE_API_BASE_URL=https://localhost:5001
